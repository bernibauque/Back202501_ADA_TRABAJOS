const concatenarNombres = function (nombre, apellido) {
    let nombreCompleto = nombre + " " + apellido; // Variable local
    return nombreCompleto;
};
console.log(concatenarNombres("Ana", "Pérez")); // Resultado esperado: "Ana Pérez"

