function test1(x, y) {
    return y - x
}

console.log(test1(10, 40));

// La función test1 toma dos argumentos, x y y, y devuelve el resultado de restar x de y.
// 'x' es 10 y 'y' es 40.

// Explicacion paso a paso:

// 1). La función test1 recibe los argumentos x = 10 y y = 40.
// 2). Dentro de la función, se realiza la operación y - x, que es 40 - 10.
// 3). El resultado de 40 - 10 es 30.
// 4). La función devuelve 30.