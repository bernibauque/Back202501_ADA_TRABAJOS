// Crear una función que imprima todos los dígitos decimales, del 0 al 9, usando un ciclo For.

function imprimirDigitos() {
    // Imprimir todos los dígitos del 0 al 9
    for (let i = 0; i <= 9; i++) {
        console.log(i);
    }
}

imprimirDigitos();
